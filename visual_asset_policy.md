# 验证版美术资源策略

## 当前决定

验证版直接复用参考 Mod `tfc-ore-washing-1.20.1-forge-1.0.6` 中与矿物加工有关的物品模型和贴图，不在此阶段生成或重绘替代素材。

复用范围仅限验证流程可读性所需的资源，例如碎矿、矿粉和压块的模型与纹理。资源文件会保留在独立的 `assets/tfc_oreprocess` 路径下，游戏代码只引用本 Mod 的资源位置，不依赖参考 JAR 在运行时存在。

## 后续替换

当项目确定一个许可证更宽松的开源 Mod 或素材来源后，替换只发生在资源层：模型 JSON、PNG 纹理、语言中的显示名称，以及必要时的物品模型映射。矿物数据组件、粉碎、洗矿、压块、熔炼和配方逻辑不得因素材替换而改变。

## 发布边界

当前素材仅用于本地验证，不将其视为正式发布资源。正式对外发布前，必须由项目方确认替代素材来源、许可证、作者署名和发布要求。
